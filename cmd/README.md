# goany CLI

This directory contains the CLI entry point for the goany compiler.

The compiler library is located in `../compiler/`.

## Building

```bash
make build
```

## Usage

```bash
./goany -source=[directory] -output=[name] -backend=[backend]
```

See the main [README](../README.md) for full documentation.
